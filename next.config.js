/** @type {import('next').NextConfig} */
const nextConfig = {
  // Empty Turbopack config to silence migration warning
  // Next.js excludes test files by default, no custom config needed
  turbopack: {},
};

module.exports = nextConfig;
